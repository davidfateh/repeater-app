apiVersion: v1
kind: Service
metadata:
  name: {{ include "pensieve.fullname" . }}
  labels:
    {{- include "pensieve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pensieve.selectorLabels" . | nindent 4 }}
