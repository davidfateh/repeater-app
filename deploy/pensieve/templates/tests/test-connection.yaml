apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pensieve.fullname" . }}-test-connection"
  labels:
    {{- include "pensieve.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pensieve.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
